// Generated by ios-test from trace schema v1. Do not edit by hand.

describe('ios-test generated flows', () => {
  it('onboarding', async () => {
    // Exercises: OnboardingScreen, finishOnboarding, ItemListScreen
    await device.launchApp({ newInstance: true });
    await element(by.id('onboarding-continue-btn')).tap();
    await expect(element(by.id('Items'))).toExist();
  });

  it('browse_items', async () => {
    // Exercises: ItemListScreen, itemStore.fetch, DetailScreen
    await element(by.id('item-row-1')).tap();
    await expect(element(by.id('Back to list'))).toExist();
    await element(by.id('back-to-list-btn')).tap();
    await expect(element(by.id('Items'))).toExist();
  });

  it('create_item', async () => {
    // Exercises: CreateItemScreen, createItem, itemStore.add
    await element(by.id('add-item-btn')).tap();
    await expect(element(by.id('Title'))).toExist();
    await element(by.id('title-field')).typeText('Hackathon trace');
    await expect(element(by.id('Save'))).toExist();
    await element(by.id('save-item-btn')).tap();
    await expect(element(by.id('Saved confirmation'))).toExist();
  });

  it('settings', async () => {
    // Exercises: SettingsScreen
    await element(by.id('settings-btn')).tap();
    await expect(element(by.id('Seeded data, local-only mode'))).toExist();
    await element(by.id('settings-back-btn')).tap();
    await expect(element(by.id('Items'))).toExist();
  });

});
